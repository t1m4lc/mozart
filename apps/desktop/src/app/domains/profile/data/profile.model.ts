// Placeholder shape for v0.1.0-beta.1. v0.1.0 fleshes this out with user
// preferences, account info, and the wider settings tree. The profile
// domain owns the `/settings` route and its sub-content; for v0.1.0-beta.1 the
// only content is the Anthropic connection card (see `connection.model`).
export interface Profile {
  id: string;
}
