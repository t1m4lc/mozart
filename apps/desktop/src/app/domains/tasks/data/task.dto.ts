// Raw shape returned by Tauri. Re-exported under a domain-local name so
// no other file in this domain reaches into `core/_bindings`.

export type { Task as TaskDto } from '../../../core/_bindings';
