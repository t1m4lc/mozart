// Public surface of `shell/`. Domains may reach in for the layout
// primitives + dimension constants they consume; everything else
// (shell-side-panel, panel widths used only by shell-left/right) stays
// internal.

export { ShellTopBar } from './shell-top-bar';
export { WORKSPACE_PROCESSES_PANEL_HEIGHT } from './shell-panel.constants';
