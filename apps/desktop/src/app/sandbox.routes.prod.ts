import { Route } from '@angular/router';

// Production replacement for sandbox.routes.ts. Empty array means the
// sandbox lazy imports never reach the prod bundle (no chunks emitted).
export const sandboxRoutes: Route[] = [];
