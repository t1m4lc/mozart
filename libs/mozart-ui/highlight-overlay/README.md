# highlight-overlay

Guided-tour overlay primitive. Renders a full-screen dim with a
punch-hole over a target element + a tooltip card positioned near it.
Skip / Next / Esc handled internally. Used by Phase 6's `/tour` route.
