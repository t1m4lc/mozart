# diff-stats

Compact `+added / −removed` chip in monospace, emerald for additions and
red for deletions. Renders nothing when both counts are zero. Used in
the sidebar workspace row to surface diff size vs the base branch.
